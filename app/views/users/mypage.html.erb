<div class="container">
  <h2 class="bg-body-secondary rounded-0 px-3 py-1 mt-3 mb-4 d-inline-block fs-4">マイページ</h2>
  <div class="row">
    <div class="col-md-6">
      <table class="table table-bordered">
        <tbody>
          <tr>
            <th class="bg-light">ユーザー名</th>
            <td><%= @user.name %></td>
          </tr>
          <tr>
            <th class="bg-light">メールアドレス</th>
            <td><%= @user.email_address %></td>
          </tr>
        </tbody>
      </table>
      <%= link_to "編集", users_edit_path, class: "btn btn-primary btn-lg" %>
    </div>
  </div>
  <div class="row mt-5">
    <div class="col-md-6">
      <h3>あなたの募集</h3>
      <ul class="list-group">
        <% @posts.each do |post| %>
          <li class="list-group-item">
            <%= link_to post.title, post_path(post) %>
            <span class="text-muted">
              (<%= post.prefecture %> / <%= post.event_at.strftime("%Y年%m月%d日 %H:%M") %>)</span>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>